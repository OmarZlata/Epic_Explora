import 'package:flutter/material.dart';

class GuideForgetPassword extends StatelessWidget {
  const GuideForgetPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
