import 'package:flutter/material.dart';

class GuideVerification extends StatelessWidget {
  const GuideVerification({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
