import 'package:flutter/material.dart';

class GuideResetPassword extends StatelessWidget {
  const GuideResetPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
